# 词战长空 · SkyWords

一款内置在「词忆 CET」里的单词弹幕小游戏：把四六级词库变成从天上落下的敌机，
用你的词汇量把它们打下来。**碎片时间练 3 分钟，答错不扣分只扣血。**

访问路径：站内底部/顶部导航「词战」，或直接打开 `#/game`。

---

## 玩法

### 核心循环
- 敌机从天而降，每架敌机 = 一道四选一题。
- **作答窗口 8 秒起，随波次递减（−0.25 秒/波，下限 4 秒）**；倒计时环进入最后 2 秒会变红并脉冲。
- 作答窗口内选对 → 敌机爆炸、得分、连击 +1；超时或选错 → 敌机突进到防线、扣血、连击清零。
- **答错绝不结束游戏，只扣血；生命归零才结算。**
- 每次判定后必弹「记忆强化卡」，展示单词、音标、词性、释义、例句、词根助记，答错时额外用红色标出正确释义。

### 三种题型 + 混合模式
| 模式 | 敌机显示 | 你的选择 |
|------|---------|---------|
| 英→中（默认） | 英文单词 | 4 个中文释义 |
| 中→英 | 中文释义 | 4 个英文单词 |
| 听音辨词 | 仅 🔊（不显示单词/释义） | 4 个英文单词 |
| 混合模式 | 三种每波轮换 | 随题型 |

### 难度曲线
- 敌机下落速度：40 px/s 起，**+4 px/s / 波**，上限 140 px/s。
- 每波敌机数：3 架起，**每 2 波 +1**，上限 8 架。
- 出题计时：8.0 s 起，**−0.25 s / 波**，下限 4.0 s。
- 前几波以大纲高频简单词为主，随波次逐步引入更难的词。

### 得分与扣血
- 单选正确得分：基础 100 分 × 连击加成（×1 起，**×3 封顶**）。
- 扣血公式：`damage = clamp(round(1 + 2 × fs/100), 1, 3)`，其中 `fs` 为该词热频分。
  - `fs < 25` → 扣 1 点（低频长尾词）
  - `25 ≤ fs < 75` → 扣 2 点（中频主力）
  - `fs ≥ 75` → 扣 3 点（高频必考词，答错代价最重）
- 初始生命 3 点。

### 操作
| 输入 | 移动战机 | 作答 |
|------|---------|------|
| 鼠标 | 移动鼠标左右跟随 | 点击弹药按钮 |
| 触摸 | 拖动战机左右移动 | 点击弹药按钮 |
| 键盘 | `←` / `→` 方向键 | `1` / `2` / `3` / `4` |

其他：`Esc` 暂停；切后台或鼠标移出画面会自动暂停，**恢复时不补扣时间**。

---

## 为什么游戏里的"记住了"和词库页的"错词"不一致？

游戏使用**独立的错词本**（键 `cetyi.game.wrongbook.v1`），与词库页的错词本**完全隔离**：

| | 词库页 / 复习 | 词战长空 |
|---|---|---|
| 移出条件 | 连续答对 **3** 次 | 连续答对 **2** 次 |
| 数据存储 | 主学习数据 `cetyi.v1` | 独立键 `cetyi.game.wrongbook.v1` |

**原因**：游戏是碎片时间的轻量场景，一个词一局内通常只出现 1–2 次，
因此采用更宽松的 2 次阈值，让你能在一局内真正"消灭"一个顽固词。
正式复习的 3 次标准更严谨，用于长期记忆固化。

**重要**：游戏**只读取**你的学习数据，**绝不写入**。
玩一局游戏不会改变你的 XP、连胜、徽章、复习计划或学习统计。
游戏里"记住"的词，需要你在词库页正式复习一遍才会从站点错词本移出。

---

## 只读声明（数据安全）

- 游戏对主学习数据 `cetyi.v1` **全程只读**，不调用 `store.grade()` 或任何会写学习数据的接口。
- 玩一局游戏**不会**改变你的 **XP / 连胜 / 徽章 / 复习计划 / 学习统计**。
- 仅新增一个存储键：`cetyi.game.wrongbook.v1`（游戏错词本）。**不覆盖、不删除任何既有键。**
- 结束面板的「回到词库」按钮只是把你自己导流到 `/library`，**由你自主决定**是否在正规学习流里复习。

| 存储键 | 内容 | 谁写 |
|--------|------|------|
| `cetyi.v1` | 主学习数据 | 只读（游戏绝不写） |
| `cetyi.game.wrongbook.v1` | 游戏错词本 | 游戏唯一写入方 |

---

## 无障碍

- 所有按钮都有语义化 `aria-label`；移动端热区 ≥ 44×44 px。
- 正确 / 错误用**颜色 + 图标 + 文字**三重编码（不只靠颜色）：
  - 答对 → 绿 + ✓ + 「正确」
  - 答错 → 红 + ✕ + 「正确释义是…」
- 尊重系统 `prefers-reduced-motion: reduce`：关闭粒子、抖动与缩放动画，仅保留必要的淡入淡出；强化卡停留时长不变。
- `Tab` 可遍历、`Enter` 可激活所有按钮；`1–4` 直接作答，`←`/`→` 移动。
- 亮 / 暗主题下画面与 UI 均走站点 CSS 变量，自动跟随主题，不硬编码色值。

---

## 技术说明（开发者）

- **纯 Canvas 2D + DOM 覆盖层**，零新增依赖（不引入游戏引擎 / 物理库）。
- **三层状态分离**：高频实体坐标只在引擎私有字段（不进 React）；HUD 数值仅在离散事件（判定 / 扣血 / 波次切换）与 10 Hz 节流时推送；强化卡 / 结束面板走事件通道。**每帧零 `setState`。**
- **Canvas 取色**：`getComputedStyle` 只在挂载与主题切换时读取一次并缓存（`src/game/theme.ts`），**每帧零 `getComputedStyle`**；`<html>.dark` 变化经 `MutationObserver` 自动失效重取。
- 模块边界：`theme.ts` 是唯一碰 `getComputedStyle` 的模块；`wrongbook.ts` 是唯一碰 `localStorage` 的模块；`engine.ts` 为纯逻辑类（无 React / 无 JSX）。
- 生命周期清理：`cancelAnimationFrame` + `ResizeObserver.disconnect` + `MutationObserver`（引用计数）+ 全部 `removeEventListener` + `stopSpeak()`。

### 文件清单
| 路径 | 职责 |
|------|------|
| `src/game/types.ts` | 游戏域类型定义（零运行时） |
| `src/game/constants.ts` | 难度曲线、分数、存储键、输入映射 |
| `src/game/theme.ts` | CSS 变量 → Canvas 真实色值（缓存 + Observer） |
| `src/game/wordPool.ts` | 词池过滤 + 难度分层 + 选词 |
| `src/game/distractors.ts` | 干扰项 5 级降级链 + 语义重叠过滤 |
| `src/game/wrongbook.ts` | 游戏错词本读写（连对 2 次移出） |
| `src/game/engine.ts` | 纯逻辑引擎（主循环 / 判定 / 扣血 / 波次） |
| `src/game/useGameWords.ts` | 词池派生 Hook（`useMemo`，不加载） |
| `src/game/SkyWords.tsx` | React 渲染层（Canvas + HUD + 覆盖层） |
| `src/pages/Game.tsx` | 页面薄壳（懒加载入口） |
